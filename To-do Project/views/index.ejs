<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Todo App</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    * {
      box-sizing: border-box;
      font-family: system-ui, sans-serif;
    }

    body {
      background: #f3f4f6;
      padding: 30px;
    }

    .card {
      max-width: 550px;
      margin: auto;
      background: white;
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.08);
    }

    h2 {
      text-align: center;
      margin-bottom: 20px;
    }

    form {
      display: flex;
      gap: 10px;
      margin-bottom: 20px;
      flex-wrap: wrap;
    }

    input {
      padding: 10px;
      border-radius: 8px;
      border: 1px solid #d1d5db;
      font-size: 14px;
      flex: 1;
    }

    button {
      padding: 10px 16px;
      border-radius: 8px;
      border: none;
      background: #4f46e5;
      color: white;
      cursor: pointer;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    li {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 12px;
      border-bottom: 1px solid #e5e7eb;
    }

    .task {
      display: flex;
      flex-direction: column;
    }

    .task-name {
      font-size: 15px;
    }

    .completed {
      text-decoration: line-through;
      color: #6b7280;
    }

    .date {
      font-size: 12px;
      color: #6b7280;
    }

    .actions a {
      margin-left: 10px;
      text-decoration: none;
      font-size: 13px;
      color: #4f46e5;
    }

    .empty {
      text-align: center;
      color: #6b7280;
      font-size: 14px;
    }
  </style>
</head>
<body>

<div class="card">
  <h2>Todo List</h2>

  <form action="/addTodo" method="POST">
    <input type="text" name="task" placeholder="Task name" required>
    <input type="date" name="dueDate" required>
    <button>Add</button>
  </form>

  <ul>
    <% if (allTodos.length === 0) { %>
      <div class="empty">No tasks added</div>
    <% } %>

    <% for (let i = 0; i < allTodos.length; i++) { %>
      <li>
        <div class="task">
          <span class="task-name <%= allTodos[i].completed ? 'completed' : '' %>">
            <%= allTodos[i].task %>
          </span>
          <span class="date">
            Due: <%= allTodos[i].dueDate %>
          </span>
        </div>

        <div class="actions">
          <a href="/toggleTodo?Id=<%= allTodos[i].Id %>">
            <%= allTodos[i].completed ? 'Undo' : 'Done' %>
          </a>
          <a href="/editTodo?Id=<%= allTodos[i].Id %>">Edit</a>
          <a href="/deleteTodo?Id=<%= allTodos[i].Id %>">Delete</a>
        </div>
      </li>
    <% } %>
  </ul>
</div>

</body>
</html>
