<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Management | Update Details</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <style>
        :root {
            /* Using a warm amber gradient to signify 'edit/update' mode */
            --update-gradient: linear-gradient(135deg, #f6d365 0%, #fda085 100%);
            --glass-bg: rgba(255, 255, 255, 0.95);
        }

        body {
            background: #fdfbfb;
            background-image: 
                radial-gradient(at 0% 0%, rgba(253, 160, 133, 0.1) 0, transparent 50%), 
                radial-gradient(at 100% 100%, rgba(246, 211, 101, 0.1) 0, transparent 50%);
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding: 40px 0;
        }

        .update-card {
            max-width: 650px;
            margin: auto;
            background: var(--glass-bg);
            padding: 45px;
            border-radius: 24px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.08);
            border: 1px solid rgba(255, 255, 255, 0.5);
            position: relative;
        }

        .update-card::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px;
            background: var(--update-gradient);
            border-radius: 24px 24px 0 0;
        }

        .header-section {
            margin-bottom: 35px;
            text-align: center;
        }

        .header-section h1 {
            font-size: 26px;
            font-weight: 800;
            color: #2d3436;
            margin-bottom: 10px;
        }

        .form-label {
            font-weight: 600;
            color: #4a5568;
            font-size: 0.85rem;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .form-label i {
            color: #fda085;
        }

        .form-control, .form-select {
            border-radius: 12px;
            padding: 12px 16px;
            border: 1px solid #e2e8f0;
            background-color: #f8fafc;
            transition: all 0.2s ease;
        }

        .form-control:focus, .form-select:focus {
            background-color: #fff;
            border-color: #fda085;
            box-shadow: 0 0 0 4px rgba(253, 160, 133, 0.15);
            outline: none;
        }

        .input-group-custom {
            background: #f8fafc;
            padding: 20px;
            border-radius: 12px;
            border: 1px solid #e2e8f0;
        }

        .btn-update {
            background: var(--update-gradient);
            border: none;
            padding: 14px;
            font-weight: 700;
            border-radius: 12px;
            width: 100%;
            margin-top: 20px;
            color: white;
            letter-spacing: 0.5px;
            transition: transform 0.2s, box-shadow 0.2s;
        }

        .btn-update:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 15px rgba(253, 160, 133, 0.3);
            color: white;
        }

        .back-btn {
            display: inline-flex;
            align-items: center;
            text-decoration: none;
            color: #718096;
            font-weight: 600;
            font-size: 0.9rem;
            margin-bottom: 25px;
            transition: color 0.2s;
        }

        .back-btn:hover {
            color: #fda085;
        }

        .form-check-input:checked {
            background-color: #fda085;
            border-color: #fda085;
        }

        .input-group-text {
            border-radius: 12px 0 0 12px;
            background-color: #f1f5f9;
            border: 1px solid #e2e8f0;
            color: #64748b;
        }
    </style>
</head>

<body>

    <div class="container">
        <div class="update-card">
            
            <a href="/employee/allEmployeePage" class="back-btn">
                <i class="bi bi-arrow-left me-2"></i> Back to Employee List
            </a>

            <div class="header-section">
                <h1>Edit Employee Details</h1>
                <p class="text-muted small">Update information for Employee ID: <strong>#<%= singleEmp['id'] %></strong></p>
            </div>

            <form method="post" action="/employee/updateEmp">
                <input type="hidden" name="emp_id" value="<%= singleEmp['id'] %>">

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label class="form-label"><i class="bi bi-person"></i> Full Name</label>
                        <input type="text" name="emp_name" value="<%= singleEmp['emp_name']%>" class="form-control" placeholder="Name">
                    </div>

                    <div class="col-md-6 mb-3">
                        <label class="form-label"><i class="bi bi-envelope"></i> Email Address</label>
                        <input type="email" name="emp_email" value="<%= singleEmp['emp_email']%>" class="form-control" placeholder="Email">
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label"><i class="bi bi-shield-lock"></i> Password</label>
                    <input type="password" name="emp_password" value="<%= singleEmp['emp_password']%>" class="form-control" placeholder="Password">
                </div>

                <div class="mb-3">
                    <label class="form-label"><i class="bi bi-gender-ambiguous"></i> Gender Selection</label>
                    <div class="input-group-custom">
                        <div class="form-check form-check-inline">
                            <input type="radio" name="emp_gender" value="Male" id="male" class="form-check-input" <% if(singleEmp['emp_gender']=='Male' ) { %> checked <% }%>>
                            <label class="form-check-label" for="male">Male</label>
                        </div>
                        <div class="form-check form-check-inline">
                            <input type="radio" name="emp_gender" value="Female" id="female" class="form-check-input" <% if(singleEmp['emp_gender']=='Female' ) { %> checked <% }%>>
                            <label class="form-check-label" for="female">Female</label>
                        </div>
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label"><i class="bi bi-currency-dollar"></i> Employee Salary</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="bi bi-cash"></i></span>
                        <input type="number" name="emp_salary" value="<%= singleEmp['emp_salary']%>" class="form-control" placeholder="Amount">
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label"><i class="bi bi-controller"></i> Employee Hobbies</label>
                    <div class="input-group-custom">
                        <div class="row">
                            <% ["Reading", "Music" , "Cricket" , "Uno (Game)" , "Other" ].map((hobby)=> { %>
                                <div class="col-6 col-md-4 mb-2">
                                    <div class="form-check">
                                        <input type="checkbox" name="emp_hobby" value="<%= hobby %>" id="check-<%= hobby %>" <% if(singleEmp['emp_hobby'].includes(hobby)) {%> checked <%}%> class="form-check-input">
                                        <label class="form-check-label" for="check-<%= hobby %>">
                                            <%= hobby %>
                                        </label>
                                    </div>
                                </div>
                            <% }) %>
                        </div>
                    </div>
                </div>

                <div class="mb-4">
                    <label class="form-label"><i class="bi bi-briefcase"></i> Employee Role</label>
                    <select class="form-select" name="emp_role">
                        <% ["Owner", "Manager" , "HR" , "Employee" ].map((role)=> {%>
                            <option value="<%= role %>" <%if(singleEmp['emp_role']==role) {%> selected <%}%>>
                                <%= role %>
                            </option>
                        <% }) %>
                    </select>
                </div>

                <button type="submit" class="btn btn-update">
                    <i class="bi bi-check2-circle me-2"></i> Save Updated Changes
                </button>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>