<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Management | Add Employee</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #007bff 0%, #0056b3 100%);
            --glass-bg: rgba(255, 255, 255, 0.9); 
        }

        body {
            background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), 
                        url('https://images.unsplash.com/photo-1497215728101-856f4ea42174?auto=format&fit=crop&w=1920&q=80');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            
            font-family: 'Poppins', sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding: 60px 0;
            margin: 0;
            position: relative; /* Position relative for the home button */
        }

        /* --- NEW HOME BUTTON STYLE --- */
        .home-btn {
            position: absolute;
            top: 20px;
            left: 20px;
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            padding: 10px 20px;
            border-radius: 50px;
            text-decoration: none;
            color: #1a1a1a;
            font-weight: 600;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 8px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
            transition: all 0.3s ease;
            z-index: 1100;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .home-btn:hover {
            transform: translateX(5px);
            background: #fff;
            color: #007bff;
        }
        /* ----------------------------- */

        .form-container {
            max-width: 550px;
            margin: auto;
            background: var(--glass-bg);
            backdrop-filter: blur(15px); 
            -webkit-backdrop-filter: blur(15px);
            padding: 45px;
            border-radius: 25px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .form-header h1 {
            font-size: 28px;
            font-weight: 700;
            color: #1a1a1a;
            margin-bottom: 8px;
        }

        .header-subtitle {
            color: #666;
            font-size: 0.95rem;
            margin-bottom: 30px;
        }

        .form-label {
            font-weight: 600;
            color: #444;
            font-size: 0.85rem;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .form-label i {
            color: #007bff;
        }

        .form-control, .form-select {
            border-radius: 12px;
            padding: 12px 16px;
            border: 1px solid #ddd;
            background-color: #fff;
            transition: 0.3s;
        }

        .form-control:focus, .form-select:focus {
            border-color: #007bff;
            box-shadow: 0 0 0 4px rgba(0, 123, 255, 0.15);
        }

        .options-group {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 12px;
            border: 1px solid #eee;
        }

        .btn-submit {
            background: var(--primary-gradient);
            border: none;
            padding: 15px;
            font-weight: 600;
            border-radius: 12px;
            width: 100%;
            margin-top: 20px;
            color: white;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 123, 255, 0.3);
        }

        .btn-submit:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(0, 123, 255, 0.4);
            filter: brightness(1.1);
        }

        .view-link {
            display: block;
            text-align: center;
            margin-top: 25px;
            text-decoration: none;
            color: #666;
            font-weight: 500;
            transition: 0.3s;
        }

        .view-link:hover {
            color: #007bff;
        }

        .hobby-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
        }
    </style>
</head>

<body>

    <a href="/" class="home-btn">
        <i class="bi bi-house-door-fill"></i> Home
    </a>

    <div class="container">
        <div class="form-container">
            <div class="form-header text-center">
                <h1>Add New Employee</h1>
                <p class="header-subtitle">Enter details to register a new member</p>
            </div>

            <form method="post" action="/employee/addEmp">
                
                <div class="mb-3">
                    <label class="form-label"><i class="bi bi-person"></i> Full Name</label>
                    <input type="text" name="emp_name" class="form-control" >
                </div>

                <div class="mb-3">
                    <label class="form-label"><i class="bi bi-envelope"></i> Email Address</label>
                    <input type="email" name="emp_email" class="form-control" >
                </div>

                <div class="mb-3">
                    <label class="form-label"><i class="bi bi-shield-lock"></i> Password</label>
                    <input type="password" name="emp_password" class="form-control" >
                </div>

                <div class="mb-3">
                    <label class="form-label"><i class="bi bi-gender-ambiguous"></i> Gender</label>
                    <div class="options-group">
                        <div class="form-check form-check-inline">
                            <input type="radio" name="emp_gender" value="Male" id="male" class="form-check-input">
                            <label class="form-check-label" for="male">Male</label>
                        </div>
                        <div class="form-check form-check-inline">
                            <input type="radio" name="emp_gender" value="Female" id="female" class="form-check-input">
                            <label class="form-check-label" for="female">Female</label>
                        </div>
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label"><i class="bi bi-currency-dollar"></i> Annual Salary</label>
                    <div class="input-group">
                        <span class="input-group-text bg-white border-end-0"><i class="bi bi-cash"></i></span>
                        <input type="number" name="emp_salary" class="form-control border-start-0" >
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label"><i class="bi bi-controller"></i> Hobbies</label>
                    <div class="options-group">
                        <div class="hobby-grid">
                            <% ["Reading", "Music" , "Cricket" , "Uno" , "Other" ].map((hobby)=> { %>
                                <div class="form-check">
                                    <input type="checkbox" name="emp_hobby" value="<%= hobby %>" id="hobby-<%= hobby %>" class="form-check-input">
                                    <label class="form-check-label" for="hobby-<%= hobby %>">
                                        <%= hobby %>
                                    </label>
                                </div>
                            <% }) %>
                        </div>
                    </div>
                </div>

                <div class="mb-3">
                    <label class="form-label"><i class="bi bi-image"></i> Profile Picture</label>
                    <input type="file" name="emp_image" class="form-control">
                </div>

                <div class="mb-4">
                    <label class="form-label"><i class="bi bi-briefcase"></i> Designated Role</label>
                    <select class="form-select" name="emp_role">
                        <option selected disabled>Choose a role...</option>
                        <% ["Owner", "Manager" , "HR" , "Employee" ].map((role)=> {%>
                            <option value="<%= role %>">
                                <%= role %>
                            </option>
                        <% }) %>
                    </select>
                </div>

                <button type="submit" class="btn btn-submit">
                    <i class="bi bi-person-check me-2"></i> Register Employee
                </button>
            </form>

            <a href="/employee/allEmployeePage" class="view-link">
                <i class="bi bi-arrow-left me-2"></i> Back to Employee List
            </a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html